# This dictionary maps color names to RGB values.
# The RGB values are represented as lists of three integers,
# where each integer represents the intensity of the red, green, and blue components respectively.

color: dict[str, list[int]] = {
    "black" : [0, 0, 0],    # Black color with RGB values [0, 0, 0]
    "pink" : [200, 50, 75]  # Pink color with RGB values [200, 50, 75]
}
